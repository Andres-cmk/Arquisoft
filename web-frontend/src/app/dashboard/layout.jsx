export const metadata = {
    title: "Dashboard - Crónicas de la Ciudad Blanca",
}


export default function DashboardLayout({ children }) {
	return (
        <>
        {children}
        </>
    );
}

